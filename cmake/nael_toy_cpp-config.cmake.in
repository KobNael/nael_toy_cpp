@PACKAGE_INIT@

include (CMakeFindDependencyMacro)

# ---------------------------------------------------------------------------
# Dependencies
# ---------------------------------------------------------------------------

find_dependency (Boost)
find_dependency (nael_utils)

# ---------------------------------------------------------------------------
# Targets
# ---------------------------------------------------------------------------
if (NOT TARGET nael_toy_cpp::nael_toy_cpp)
	include ("${CMAKE_CURRENT_LIST_DIR}/nael_toy_cpp-targets.cmake")
endif ()

list (PREPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/")
